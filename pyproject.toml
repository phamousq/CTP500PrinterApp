[project]
name = "ctp500printerapp"
version = "0.1.0"
description = "Bluetooth LE client for the Core Innovations CTP500 thermal printer"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.1.1",
    "bleak>=0.22.0",
]

[tool.uv]
python-preference = "only-system"
